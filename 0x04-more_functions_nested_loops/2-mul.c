#include "main.h"

/**
 * mul - Multiplies two intergers
 * @c: The first number to multiply
 * @d: The second number to multiply with first number
 * Return: The result of multiplication between c param and d param
 */
int mul(int c, int d)
{
return (c * d);
}
